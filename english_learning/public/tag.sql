/*
CREATE TABLE tags_table (
    id        INTEGER PRIMARY KEY AUTOINCREMENT,
    tag  TEXT
);
SELECT * from word

UPDATE word
SET word = 'yoga'
WHERE word = 'yoge'; 
*/

SELECT * from word;--查询全部单词