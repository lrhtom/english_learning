/*
CREATE TABLE tags_table (
    id        INTEGER PRIMARY KEY AUTOINCREMENT,
    tag  TEXT
);
SELECT * from word

UPDATE word
SET word = 'yoga'
WHERE word = 'yoge'; 

UPDATE word
SET tag = 'country'
WHERE tag = '国家'; 

UPDATE word
SET meaning = '人品'
WHERE word = 'personality'; 

ALTER TABLE word
ADD COLUMN review INTEGER NOT NULL DEFAULT 0;

DELETE from word WHERE word = 'amosphere'; 
SELECT * from word;--查询全部单词
UPDATE word
SET word = 'check'
WHERE meaning = '支票'; 

*/
SELECT * from word;
